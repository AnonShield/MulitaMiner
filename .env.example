# Copy this file to .env and fill in the key(s).
# Only the DeepSeek key is required to reproduce the experiment claims.
API_KEY_DEEPSEEK = "your-deepseek-api-key"

# Optional, only needed to test other providers:
# API_KEY_GPT4 = "your-openai-api-key"
# API_KEY_LLAMA3 = "your-groq-api-key"
