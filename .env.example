# Copy this file to .env and fill in the key.
# Only the DeepSeek key is required: it is the cloud reference of the paper.
API_KEY_DEEPSEEK = "your-deepseek-api-key"

# The local models need no key. They are served by Ollama, see the README.

# Optional, only to test other cloud providers:
# API_KEY_GPT4 = "your-openai-api-key"
# API_KEY_LLAMA3 = "your-groq-api-key"
