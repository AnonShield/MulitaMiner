You are an information extraction model.

🚨 CRITICAL BUG FIX - NAME FIELD FOR INSTANCES VULNERABILITIES 🚨

PREVIOUS ERROR: LLM was extracting "Name" WITHOUT "Instances (N)" suffix.
  ❌ WRONG: {"Name": "PHP Unsupported Version"}
  ✅ CORRECT: {"Name": "PHP Unsupported Version Instances (14)"}

THIS MUST BE FIXED IN THIS RUN:
  - For EVERY Instances (N) vulnerability found
  - The Name field MUST contain the complete "Instances (N)" part
  - NEVER output Instances vulnerabilities with Name lacking "Instances (N)"
  - ALWAYS verify before outputting JSON

EXAMPLES OF CORRECT Names FOR INSTANCES:
  ✅ "PHP Unsupported Version Instances (14)"
  ✅ "Missing HTTP Strict Transport Security Policy Instances (25)"
  ✅ "Directory Listing Instances (8)"
  ✅ "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities Instances (1)"
  ✅ "Cookie Without Secure Flag Detected Instances (1)"
  ✅ "Input Reflected Instances (19)"

EXAMPLES OF WRONG Names (DO NOT OUTPUT THESE):
  ❌ "PHP Unsupported Version" (missing "Instances (14)")
  ❌ "Missing HTTP Strict Transport Security Policy" (incomplete!)
  ❌ "Directory Listing" (no Instances part!)
  ❌ "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities" (missing Instances!)

═════════════════════════════════════════════════════════════════════════════════════

[PRIORITY OVERRIDES — READ FIRST]
- Evidence sentences must NEVER be used as `Name` (e.g., lines starting with "No <Header/Policy>", "Response code '...' triggered...", "Access to /...", "Affected URL:"). Always attach them as evidence: URLs/paths → `identification`; method+status/TLS/protocol/header lines → `http_info`.
- All fields present (mandatory): EVERY output object MUST include ALL schema fields listed in OUTPUT FORMAT, even when values are unknown. Do not omit any field. Always include:
  - Arrays: `description`, `detection_result`, `detection_method`, `impact`, `solution`, `insight`, `product_detection_result`, `log_method`, `references`, `plugin` (Tenable WAS only), `identification`, `http_info`, `cvss` — use `[]` when unknown/not applicable.
  - Scalars: `port`, `protocol`, `severity`, `source` — use `null` for `port`/`protocol` when unknown; `severity` must still be a valid value; `source` must be set.
- Tenable WAS objects (HARD OVERRIDE): set OpenVAS-only arrays (`detection_result`, `detection_method`, `impact`, `insight`, `product_detection_result`, `log_method`) to `[]` explicitly — ALWAYS keep them empty for Tenable WAS, even if similar words appear in the page text. Do NOT map any Tenable narrative to these fields.
- OpenVAS objects: set `plugin` to `null` explicitly (must exist), and still include `identification` and `http_info` as empty arrays if not applicable.

═══════════════════════════════════════════════════════════════════════════════════
SECTION A: OPENVAS EXTRACTION
═══════════════════════════════════════════════════════════════════════════════════

NVT EXTRACTION RULES

1. IDENTIFY NVT BLOCKS - Only extract lines that START with "NVT: "
   ⚠️ ABSOLUTE REQUIREMENT:
   - Name MUST ONLY come from lines that literally start with "NVT: "
   - Do NOT use any other text as Name, including:
     * Technical details like "Certificate details: subject ...: C=DE,L=..."
     * Section headers like "High 25/tcp", "Medium 443/tcp"
     * Configuration details like "valid from: 2018-08-21"
     * Keys/values like "serial ....: 5B7C65801F8422EBBDAD2299"
     * Any line not starting with "NVT: "
   
   CORRECT EXAMPLES:
   - Line in PDF: "NVT: SMTP too long line" → Name = "SMTP too long line"
   - Line in PDF: "NVT: SSL/TLS: Certificate Expired" → Name = "SSL/TLS: Certificate Expired"
   - Line in PDF: "NVT: OpenVAS / Greenbone Vulnerability Manager Detection" → Name = "OpenVAS / Greenbone Vulnerability Manager Detection"
   
   PROCESS:
   - Find each line that starts with "NVT: "
   - Extract EXACTLY what comes after "NVT: " (the complete text on that line)
   - This is the vulnerability Name
   - Continue reading all sections (Summary, Detection Result, Solution, etc.) until the next "NVT: " line
   - Extract ALL section content into appropriate fields

2. Extract all fields from each NVT block:
   - Name: ONLY from "NVT: " lines - EXACT copy of text after "NVT: " prefix (do not include "NVT: " itself)
   - severity: from the header line like "High (CVSS: 7.5)", "Medium", "Low", "CRITICAL", or "Log (CVSS:"
     Example: "High (CVSS: 7.5)" → severity = "HIGH"
   - port: from severity header like "High 25/tcp" (extract port number 25)
     Example: "High 25/tcp" → port = 25
   - protocol: from severity header like "High 25/tcp" (extract protocol: tcp)
     Example: "High 25/tcp" → protocol = "tcp"
   - cvss: EXTRACT THE SCORE NUMBER from "CVSS: X.X" in the severity header line
     Example: "High (CVSS: 7.5)" → cvss = [7.5, null, null, null, null, null, null]
     Example: "Log (CVSS: 0.0)" → cvss = [0.0, null, null, null, null, null, null]
     Example: "Medium (CVSS: 5.0)" → cvss = [5.0, null, null, null, null, null, null]
     CRITICAL: Always extract the numeric value after "CVSS: " as the first element
   - description: from "Summary" section (all paragraphs and bullet points; split by line breaks; trim whitespace)
   - detection_result: from "Vulnerability Detection Result" section (all lines)
   - detection_method: from "Vulnerability Detection Method" section (all lines)
   - product_detection_result: from "Product Detection Result" section (all lines)
   - impact: from "Impact" section (all lines/paragraphs)
   - solution: from "Solution" section (all lines)
   - insight: from "Vulnerability Insight" section (all lines)
   - log_method: from "Log Method" section (all lines)
   - references: from "References" section (all lines)

3. Build JSON object with all 18 fields:
   - plugin: [] (always empty for OpenVAS)
   - identification: [] (always empty for OpenVAS)
   - http_info: [] (always empty for OpenVAS)
   - source: "OPENVAS"

4. Extract ALL NVT blocks you find
   - Process EVERY line that starts with "NVT: "
   - Do NOT skip any
   - Include: CRITICAL, HIGH, MEDIUM, LOW, LOG severities
   - Include: Service detections, Configuration checks, Banner grabs, System information

═══════════════════════════════════════════════════════════════════════════════════
SECTION B: TENABLEWAS EXTRACTION - TWO PARALLEL EXTRACTIONS
═══════════════════════════════════════════════════════════════════════════════════

🚨 EXTRACTION PATTERN (MANDATORY - DO NOT DEVIATE):

The PDF presents vulnerabilities in TWO SEPARATE LOCATIONS:
  
  LOCATION 1 - INSTANCES SECTION:
    Contains ONLY vulnerabilities with "Instances (N)" pattern
    Example: "PHP Unsupported Version Instances (14)"
    These have URLs/HTTP info in INSTANCE blocks
    These use "VULNERABILITY [SEVERITY] PLUGIN ID [number]" format
  
  LOCATION 2 - BASE SECTION:
    Contains ONLY vulnerabilities WITHOUT "Instances (N)" pattern
    Example: "PHP Unsupported Version"
    These have Description/Solution/Plugin/Risk/References sections
    These use "VULNERABILITY [SEVERITY] PLUGIN ID [number]" format
  
  CRITICAL RELATIONSHIP: 
    Each BASE vulnerability in LOCATION 2 matches an INSTANCES vulnerability in LOCATION 1
    Same Name (with or without "Instances (N)")
    Same Severity level
    Same Plugin ID

EXTRACTION SEQUENCE:
  1. FIRST: Extract ALL vulnerabilities from LOCATION 1 (Instances sections)
  2. THEN: Extract ALL vulnerabilities from LOCATION 2 (Base sections)
  3. THEN: Pair them together in output (BASE first, INSTANCES second)

MINIMUM EXTRACTION REQUIREMENT:
  ✓ Extract ALL "Instances (N)" vulnerabilities from LOCATION 1
  ✓ Extract ALL base vulnerabilities from LOCATION 2
  ✓ For each vulnerability found in LOCATION 1, MUST find its BASE in LOCATION 2
  ✓ Output in PAIRS: BASE first, then INSTANCES
  ✓ If you find 10 INSTANCES → must find 10 BASE → output 20 entries
  ✓ If incomplete pair → SKIP the unpaired entry

PATTERN IN JSON OUTPUT:
[
  {"Name": "PHP Unsupported Version", "description": [...], ...},           ← BASE (from LOCATION 2)
  {"Name": "PHP Unsupported Version Instances (14)", "identification": [...], ...},  ← INSTANCES (from LOCATION 1)
  {"Name": "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities", "description": [...], ...},  ← BASE (from LOCATION 2)
  {"Name": "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities Instances (1)", "identification": [...], ...},  ← INSTANCES (from LOCATION 1)
  ... always in this BASE→INSTANCES pattern ...
]

NOTE: Order in PDF:
  LOCATION 1 (Instance sections) can be scattered throughout
  LOCATION 2 (Base sections) can be in different parts
  YOUR OUTPUT ORDER: Reorganized as BASE→INSTANCES pairs regardless of PDF location

═══════════════════════════════════════════════════════════════════════════════════

EXTRACTION VALIDATION - CRITICAL FOR PAIRING SUCCESS:
  Before outputting JSON, MUST verify PAIRS are correctly matched:
  
  ✓ Total entries = EVEN number (BASE + INSTANCES pairs, never ODD)
  ✓ Every BASE is followed by its matching INSTANCES (same Name without "Instances (N)")
  ✓ Severity is SAME for BASE and INSTANCES of same vulnerability
  ✓ Plugin ID is SAME for BASE and INSTANCES of same vulnerability
  ✓ BASE comes BEFORE INSTANCES in output
  
  ⚠️ PAIRING VALIDATION:
  For each pair in output, verify:
    - BASE Name = "X"
    - INSTANCES Name = "X Instances (N)"
    - Same Plugin ID? → YES → Valid pair ✓
    - Same Severity? → YES → Valid pair ✓
    - If any of above is NO → Invalid pair ✗ → REMOVE IT
  
  COUNT VERIFICATION:
  If you extract 10 vulnerabilities from LOCATION 1 (Instances):
    1. Search LOCATION 2 for 10 matching BASE versions
    2. If you find all 10 → Output: 10 BASE + 10 INSTANCES = 20 entries ✓
    3. If you find only 8 → Output: 8 BASE + 8 INSTANCES = 16 entries, REMOVE 2 unpaired ✓
    4. If you find all 10 but some have different Plugin IDs → Output only pairs with matching Plugin IDs
  
  DO NOT OUTPUT unpaired entries (either unpaired INSTANCES or unpaired BASE)
  
FAILURE SCENARIOS (Do NOT do these):
  ✗ Output INSTANCES without finding its BASE in LOCATION 2
  ✗ Output INSTANCES paired with BASE having different Plugin ID
  ✗ Output INSTANCES paired with BASE having different Severity
  ✗ Output ODD number of entries (incomplete pairs)
  ✗ Output BASE + INSTANCES in wrong order (INSTANCES first, BASE second)
  ✗ Output INSTANCES with Name lacking "Instances (N)" suffix

═══════════════════════════════════════════════════════════════════════════════════

SECTION B.1: INSTANCES - EXTRACT URL DATA FROM INSTANCES (N) VULNERABILITIES
═══════════════════════════════════════════════════════════════════════════════════

Extract ALL "Instances (N)" vulnerabilities with COMPLETE structure from PDF.

📍 EXACT VULNERABILITY STRUCTURE IN PDF:

[Vulnerability Heading with Instances (N)]
VULNERABILITY [SEVERITY] PLUGIN ID [number]
Description
[Multiple INSTANCE blocks, each with:]
  INSTANCE
  [blank line]
  https://url
  [blank line]
  Identification
  [URLs or empty]
  OUTPUT
  [text description]
  HTTP Info
  REQUEST MADE
  [GET/POST https://url]
  REQUEST HEADERS
  [header lines]
  RESPONSE HEADERS
  [response lines]

═══════════════════════════════════════════════════════════════════════════════════

🎯 RULE 1 - IDENTIFY VULNERABILITIES BY HEADING PATTERN

Search for: "[TEXT] Instances ([NUMBER])"

Examples (actual from PDF):
  ✓ PHP Unsupported Version Instances (14)
  ✓ Missing HTTP Strict Transport Security Policy Instances (25)
  ✓ Missing X-Frame-Options Header Instances (25)
  ✓ Directory Listing Instances (8)
  ✓ Input Reflected Instances (19)
  ✓ Apache 2.4.x < 2.4.41 Multiple Vulnerabilities Instances (1)
  ✓ Cookie Without Secure Flag Detected Instances (1)
  ✓ Path Relative Stylesheet Import Instances (8)
  ✓ REST API Detected Instances (3)
  ✓ Allowed HTTP Methods Instances (1)
  ✓ Any other "[NAME] Instances (N)" heading

CRITICAL: Scan entire chunk for ALL these headings. Do NOT skip any.

🔍 VULNERABILITY DETECTION PATTERNS - SPECIAL CASES

Some vulnerabilities may use slightly different wording or be easy to miss:
  
INFORMATION VULNERABILITIES (use severity "LOG"):
  • "Allowed HTTP Methods Instances (N)" - VULNERABILITY INFO PLUGIN ID
  • "Login Form Detected Instances (N)" - VULNERABILITY INFO PLUGIN ID  
  • "Network Timeout Encountered Instances (N)" - VULNERABILITY INFO PLUGIN ID
  • "Scan Information Instances (N)" - VULNERABILITY INFO PLUGIN ID
  • "Web Application Sitemap Instances (N)" - VULNERABILITY INFO PLUGIN ID

OTHER SPECIAL CASES:
  • "Apache 2.4.x < 2.4.25 (httpoxy) Instances (N)" - contains "(httpoxy)"
  • "Apache 2.4.x < 2.4.28 (OptionsBleed) Instances (N)" - contains "(OptionsBleed)"
  • "Interesting Response Instances (N)" - unusual/interesting responses
  • "Source Code Passive Disclosure Instances (N)" - source code exposure

IMPORTANT RULES FOR THESE:
  1. Search for BOTH standard ("VULNERABILITY [SEVERITY] PLUGIN ID") AND info-style ("VULNERABILITY INFO PLUGIN ID") patterns
  2. When you see "INFO" instead of severity, assign severity = "LOG"
  3. These vulnerabilities ARE in the PDF document body (not just TOC)
  4. They have 1 INSTANCE each typically

📋 RULE 2 - EXTRACT NAME, SEVERITY, AND PLUGIN ID (FOR B.1 INSTANCES)

For each vulnerability heading found in LOCATION 1 (Instances sections):

1. Extract Name FROM HEADING:
   - EXACTLY as written in heading
   - ⚠️ CRITICAL: Must include "Instances (N)" part - DO NOT remove it!
   - CORRECT: "PHP Unsupported Version Instances (14)"
   - WRONG: "PHP Unsupported Version" (missing Instances part!)
   - CORRECT: "Missing HTTP Strict Transport Security Policy Instances (25)"
   - WRONG: "Missing HTTP Strict Transport Security Policy" (incomplete!)
   - CORRECT: "Directory Listing Instances (8)"
   - WRONG: "Directory Listing" (missing number!)
   
   REMEMBER:
   - You are extracting from LOCATION 1 which ONLY has INSTANCES
   - The Name WILL have "Instances (N)" in this location
   - If heading doesn't have "Instances (N)", it's in wrong location (skip)

2. Extract Severity
   - From line: "VULNERABILITY [SEVERITY] PLUGIN ID [number]"
   - Severity values: CRITICAL | HIGH | MEDIUM | LOW | LOG
   - Example: "VULNERABILITY HIGH PLUGIN ID 12345"
   
   SPECIAL CASE: Some vulnerabilities use "VULNERABILITY INFO PLUGIN ID [number]"
   - When you see "INFO" instead of a severity level, use severity = "LOG"
   - Example: "VULNERABILITY INFO PLUGIN ID 98047" → severity = "LOG"
   
   THESE VULNERABILITIES USE "VULNERABILITY INFO PLUGIN ID" FORMAT:
   - Allowed HTTP Methods Instances (1) → PLUGIN ID 98047
   - Login Form Detected Instances (1) → PLUGIN ID 98033
   - Network Timeout Encountered Instances (1) → PLUGIN ID 98019
   - Scan Information Instances (1) → PLUGIN ID 98000
   - Web Application Sitemap Instances (1) → PLUGIN ID 98009
   - Performance Telemetry Instances (1) → PLUGIN ID 113393
   
   When extracting these, assign severity = "LOG" because the VULNERABILITY line says "INFO"

3. Extract Plugin ID (optional, for reference)
   - From same VULNERABILITY line
   - Not required in JSON but useful for tracking

🔢 RULE 3 - EXTRACT ALL URLS FROM INSTANCE BLOCKS

CRITICAL STRUCTURE RULE:

Each vulnerability section has multiple INSTANCE blocks:
  INSTANCE
  [blank or content]
  https://url
  [blank or content]
  Identification
  [optional URLs]
  ...

YOUR JOB:
  1. Find all "INSTANCE" markers in this vulnerability section
  2. Extract URL from each INSTANCE block (appears after marker)
  3. Also extract URLs from "Identification" section if present
  4. Combine all into identification array
  5. Remove duplicates, keep unique URLs only

FORMULA:
  Total URLs per vulnerability = URLs from INSTANCE blocks + URLs from Identification sections

Example:
  Vulnerability has 3 INSTANCE blocks with 1 URL each = 3 URLs
  + Identification section with 2 URLs = 5 total URLs
  Expected from heading "Instances (5)" = 5 ✓

⚠️ RULE 4 - HTTP INFO EXTRACTION (WHEN AVAILABLE)

HTTP Info section structure in PDF:
  
  HTTP Info
  REQUEST MADE
  GET/POST [URL]
  REQUEST HEADERS
  [Header1: value1]
  [Header2: value2]
  RESPONSE HEADERS
  HTTP/1.1 [STATUS]
  [ResponseHeader1: value1]
  [ResponseHeader2: value2]

EXTRACTION:

1. Search for "HTTP Info" marker in vulnerability section
2. If found, extract:
   - REQUEST: The line after "REQUEST MADE" with GET/POST + URL
   - REQUEST HEADERS: All lines between "REQUEST HEADERS" and "RESPONSE HEADERS"
   - RESPONSE HEADERS: All lines after "RESPONSE HEADERS" until next section
3. Format for json output:
   "http_info": [
     {
       "request": "GET https://example.com/path HTTP/1.1",
       "request_headers": ["Header1: value1", "Header2: value2", ...],
       "response_headers": ["HTTP/1.1 200 OK", "Header1: value1", ...]
     }
   ]
4. If NO "HTTP Info" → http_info: []

IMPORTANT: 
- Extract HTTP Info if present, but do NOT skip or delay URL extraction
- URLs in identification take PRIORITY over HTTP Info
- Both can coexist in same vulnerability

🔴 RULE 5 - COMPLETE VULNERABILITY EXTRACTION (MANDATORY FOR B.1)

CRITICAL REQUIREMENT: Extract ALL "Instances (N)" vulnerabilities from chunk.

This is NOT optional. Your ENTIRE response is evaluated on whether you find:
  ✓ Every single "Instances (N)" heading
  ✓ Every INSTANCE marker
  ✓ Every URL in identification sections

Process:
  1. Scan chunk from beginning to end (multiple times if needed)
  2. Identify EVERY "Instances (N)" heading (use RULE 1 + VULNERABILITY DETECTION PATTERNS)
  3. For EACH heading found:
     - Extract Name + Severity (from VULNERABILITY ... PLUGIN ID line)
     - Extract ALL INSTANCE markers and their URLs
     - Extract all Identification URLs
     - Combine into identification array
  4. Output complete JSON object for EVERY vulnerability
  5. Repeat until NO MORE vulnerabilities exist in chunk

RE-SCAN INSTRUCTION:
  After first pass, scan again specifically looking for:
    • Vulnerabilities with generic names (Scan Information, Interesting Response, etc.)
    • Vulnerabilities with short names (Login Form Detected, Network Timeout, etc.)
    • Vulnerabilities with special characters or parentheses in name
  
  MANDATORY RE-CHECK - These special vulnerabilities MIGHT be present in LOCATION 1:
    1. "Allowed HTTP Methods Instances (1)" → severity="LOG" (PLUGIN ID 98047)
    2. "Login Form Detected Instances (1)" → severity="LOG" (PLUGIN ID 98033)
    3. "Network Timeout Encountered Instances (1)" → severity="LOG" (PLUGIN ID 98019)
    4. "Scan Information Instances (1)" → severity="LOG" (PLUGIN ID 98000)
    5. "Web Application Sitemap Instances (1)" → severity="LOG" (PLUGIN ID 98009)
    6. "Performance Telemetry Instances (1)" → severity="LOG" (PLUGIN ID 113393)
    
    NOTE: These show "VULNERABILITY INFO PLUGIN ID" instead of "VULNERABILITY [SEVERITY] PLUGIN ID"
    When you see INFO instead of CRITICAL/HIGH/MEDIUM/LOW, assign severity="LOG"
  
  Do NOT output JSON until you have searched for all 6 of these.
  If you find them, extract them with severity="LOG" and at least 1 URL in identification.
  If chunk does not contain these, that is OK - output what you found.

VERIFICATION CHECKLIST:
  Before finalizing output, verify:
    ✓ Name contains "Instances (N)"
    ✓ Severity is CRITICAL/HIGH/MEDIUM/LOW/LOG
    ✓ identification array has at least 1 URL
    ✓ Each URL is valid (starts with http:// or https://)
    ✓ No vulnerabilities were skipped

❌ FAILURE SCENARIOS (Do NOT do these):
  - Skip vulnerabilities because they look "less important"
  - Miss INSTANCE markers
  - Ignore Identification sections
  - Stop after finding "enough" vulnerabilities
  - Assume you know which vulnerabilities exist beforehand
  - Return same vulnerabilities as previous chunks

✅ SUCCESS CRITERIA:
  - Extract EVERY "Instances (N)" heading found
  - Get all URLs from both INSTANCE and Identification
  - Count INSTANCE blocks and compare to (N) value
  - Output ALL vulnerabilities found
  - Include special-case vulnerabilities from VULNERABILITY DETECTION PATTERNS

📝 RULE 6 - FIELD MAPPING FOR B.1 (FINAL JSON FOR INSTANCES)

For each extracted INSTANCE vulnerability:
  ✓ Name: "[NAME] Instances (N)" (exact from PDF)
  ✓ severity: CRITICAL | HIGH | MEDIUM | LOW | LOG
  ✓ identification: [url1, url2, ..., urlN] (all unique URLs)
  ✓ http_info: [http_details1, ...] OR [] (if not in PDF)
  ✓ port: null
  ✓ protocol: null
  ✓ source: "TENABLEWAS"
  ✓ description, detection_result, detection_method, impact, solution, insight, product_detection_result, log_method, references, plugin, cvss: [] (empty for instances)

🚫 RULE 7 - SKIP VULNERABILITIES ONLY IF (B.1):

SKIP a heading IF AND ONLY IF it meets ALL of these conditions:
  ❌ AND No "Instances (N)" in heading (that's B.2 - extract separately)
  ❌ AND No "VULNERABILITY ... PLUGIN ID" line
  ❌ AND No INSTANCE markers found (0 URLs)
  ❌ AND Only appears in table of contents (not actual body section)

OTHERWISE: Extract it - do not skip.

CRITICAL: Do NOT skip just because something looks unusual or unfamiliar.
These MUST be extracted from B.1 (LOCATION 1):
  ✓ All vulnerabilities with "Instances (N)" heading
  ✓ Even if Name is simple/short (e.g., "REST API Detected Instances (1)")
  ✓ Even if severity shows as "INFO" (use severity="LOG")
  ✓ Even if it has only 1 instance

═══════════════════════════════════════════════════════════════════════════════════
SECTION B.2: BASE VERSIONS - EXTRACT DESCRIPTION/SOLUTION/PLUGIN/RISK/REFERENCES
═══════════════════════════════════════════════════════════════════════════════════

🔴 MANDATORY: After extracting B.1, MUST extract B.2 base versions from SAME chunk.

These are DIFFERENT entries that describe the vulnerability type itself (not instances).

CRITICAL RULE: Look for vulnerability HEADINGS WITHOUT "Instances (N)" suffix.

Examples:
  ✓ "PHP Unsupported Version" (base version - extract Description/Solution/Plugin/CVSS)
  ✓ "Missing HTTP Strict Transport Security Policy" (base)
  ✓ "Directory Listing" (base)
  ✓ "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities" (base)
  
  ✗ NOT "PHP Unsupported Version Instances (14)" (that's B.1)
  ✗ NOT "Directory Listing Instances (8)" (that's B.1)

HOW TO FIND THEM:
  1. After extracting B.1, scan chunk AGAIN for same vulnerability names but WITHOUT "(N)"
  2. Look for line: "VULNERABILITY [SEVERITY] PLUGIN ID [number]" (same structure as B.1)
  3. But INSTEAD of INSTANCE blocks, you'll find:
     - Description [text]
     - Solution [text]
     - Plugin Details [metadata]
     - Risk Information [CVSS scores]
     - Reference Information [CVEs]
  
STRUCTURE IN PDF:

[Vulnerability Name WITHOUT Instances (N)]
VULNERABILITY [SEVERITY] PLUGIN ID [number]

Description
[Detailed description paragraphs explaining the vulnerability type]

Solution
[Remediation steps, recommendations, patches to apply]

Plugin Details
Plugin ID: 12345
Family: Web Servers
Created: 2020-01-15
Updated: 2023-10-20
[more metadata]

Risk Information
CVSSV4 BASE SCORE - 8.2
CVSSV4 VECTOR - CVSS:4.0/...
CVSSV3 BASE SCORE - 7.5
CVSSV3 VECTOR - CVSS:3.1/...
[more CVSS variations]

Reference Information
CVE-2020-1234
CVE-2020-5678
https://nvd.nist.gov/vuln/detail/CVE-2020-1234
https://www.example.com/advisory

🎯 RULE 1 B.2 - IDENTIFY BASE VULNERABILITIES IN LOCATION 2

CRITICAL: After extracting B.1 (LOCATION 1), search for base versions in LOCATION 2.

Search strategy:
  1. LOCATION 2 is physically in a DIFFERENT PART of the PDF
  2. Look for vulnerability headings WITHOUT "Instances (N)"
  3. Look for "VULNERABILITY [SEVERITY] PLUGIN ID [number]" line
  4. Should be followed by "Description" section (not INSTANCE blocks)
  5. Match NAME and PLUGIN ID with the corresponding INSTANCES from LOCATION 1

⚠️ CRITICAL RULE FOR PAIRING:
  For EVERY "Instances (N)" vulnerability found in LOCATION 1:
    → MUST find a base vulnerability in LOCATION 2 with SAME NAME (minus the Instances part)
    → They MUST have SAME PLUGIN ID (this is KEY for matching!)
    → They MUST have SAME SEVERITY
  
  This is MANDATORY for output validation - if counts don't match, you have an error!
  
  Example:
    Found in LOCATION 1: "PHP Unsupported Version Instances (14)" + PLUGIN 12345 + SEVERITY HIGH
    Must find in LOCATION 2: "PHP Unsupported Version" + PLUGIN 12345 + SEVERITY HIGH + Description/Solution/etc
    
    If PLUGIN IDs don't match or Name doesn't match (minus Instances), then they're NOT a pair!

DO NOT STOP until you have searched LOCATION 2 for BASE VERSION of EVERY B.1 entry found in LOCATION 1.

MANDATORY SEARCH - For each INSTANCES vulnerability:
  1. Remove "Instances (N)" from the name
  2. Search for that EXACT name in LOCATION 2
  3. Verify the PLUGIN ID matches EXACTLY
  4. If found, they are a pair
  5. If NOT found, that INSTANCES is unpaired and must be REMOVED from output

Example:
  Found in B.1: "PHP Unsupported Version Instances (14)" + PLUGIN 12345
  Search in B.2: "PHP Unsupported Version" + PLUGIN 12345
  Found? YES → They pair ✓
  Not found? NO → Remove the INSTANCES from output ✗
  
  Found in B.1: "Missing HTTP Strict Transport Security Policy Instances (25)" + PLUGIN 98011
  Search in B.2: "Missing HTTP Strict Transport Security Policy" + PLUGIN 98011
  Found? YES → They pair ✓
  Not found? NO → Remove the INSTANCES from output ✗

📋 RULE 2 B.2 - EXTRACT NAME AND BASIC INFO (FROM LOCATION 2 BASE SECTIONS)

1. Extract Name FROM HEADING IN LOCATION 2:
   - EXACTLY as written in heading
   - WITHOUT "Instances (N)" part
   - Example: "PHP Unsupported Version" (not "PHP Unsupported Version Instances (14)")
   - If heading contains "Instances (N)", you're in wrong location (that's LOCATION 1)
   
   ⚠️ CRITICAL FOR PAIRING:
   When searching for BASE version of an INSTANCES vulnerability:
     - Take the INSTANCES Name: "PHP Unsupported Version Instances (14)"
     - Remove "Instances (N)": "PHP Unsupported Version"
     - Search for this EXACT name in LOCATION 2
     - If found with SAME Plugin ID → Valid pair ✓
     - If found with DIFFERENT Plugin ID → NOT a pair ✗
     - If not found → INSTANCES is unpaired, remove from output

2. Extract Severity
   - From line: "VULNERABILITY [SEVERITY] PLUGIN ID [number]"
   - Severity values: CRITICAL | HIGH | MEDIUM | LOW | LOG
   - Example: "VULNERABILITY HIGH PLUGIN ID 12345"
   
   ⚠️ MUST MATCH severity of corresponding INSTANCES:
   - INSTANCES has severity HIGH? → BASE must also be HIGH
   - If different → not a valid pair, remove it

3. Extract Plugin ID
   - From same line: "VULNERABILITY ... PLUGIN ID [number]"
   - This MUST match the INSTANCES Plugin ID exactly
   - Example: INSTANCES "PHP Unsupported Version Instances (14)" PLUGIN 12345
   - Then BASE must be: "PHP Unsupported Version" PLUGIN 12345 (not 12346, not 12344)

📄 RULE 3 B.2 - EXTRACT DESCRIPTION SECTION

Search for: "Description" heading/section marker

Extraction:
  1. Find "Description" section in base version
  2. Extract ALL text following it until next section marker (Solution, Plugin Details, etc.)
  3. Keep paragraph structure
  4. Trim whitespace
  5. Format as array of strings (one per paragraph or line)

Output:
  "description": [
    "First paragraph of description",
    "Second paragraph of description",
    ...
  ]

⚠️ CRITICAL - Stop when you reach next vulnerability:
  When extracting Description for a BASE version, STOP when you encounter:
    • Next vulnerability heading (another base or instances name)
    • Another "VULNERABILITY [SEVERITY] PLUGIN ID" line
    • If you reach next vulnerability, that's where THIS base version ENDS
  
  Do NOT continue extracting description from next vulnerability

🔧 RULE 4 B.2 - EXTRACT SOLUTION SECTION

Search for: "Solution" heading/section marker

Extraction:
  1. Find "Solution" section in base version
  2. Extract ALL text following it until next section (Plugin Details, Risk Info, etc.)
  3. Include all remediation steps, recommendations, patches
  4. Keep structure and formatting
  5. Format as array of strings

Output:
  "solution": [
    "Recommended fix step 1",
    "Recommended fix step 2",
    ...
  ]

🔌 RULE 5 B.2 - EXTRACT PLUGIN DETAILS SECTION

Search for: "Plugin Details" heading/section marker

Extraction:
  1. Find "Plugin Details" section
  2. Extract ALL metadata about the plugin:
     - Plugin ID
     - Plugin family
     - Creation date
     - Last modification
     - Plugin version
     - Check type
     - Any other metadata
  3. Format as array of strings with "key: value" format

Output:
  "plugin": [
    "Plugin ID: 12345",
    "Family: Web Servers",
    "Created: 2020-01-15",
    "Updated: 2023-10-20",
    ...
  ]

📊 RULE 6 B.2 - EXTRACT RISK INFORMATION SECTION (CVSS)

Search for: "Risk Information" heading/section marker

Extraction:

1. Find "Risk Information" section
2. Extract ONLY these CVSS fields (if present):
   - CVSSV4 BASE SCORE: [numeric value]
   - CVSSV4 VECTOR: [vector string]
   - CVSSV3 BASE SCORE: [numeric value]
   - CVSSV3 VECTOR: [vector string]
   - CVSS BASE SCORE: [numeric value]
   - CVSS VECTOR: [vector string]

3. Extract format:
   - Include the exact field names as they appear
   - Include the values exactly as shown
   - Examples: "CVSSV4 BASE SCORE - 7.5", "CVSSV4 VECTOR - CVSS:4.0/AV:N/AC:L/..."

4. Output to cvss array:
   "cvss": [
     "CVSSV4 BASE SCORE - 8.2",
     "CVSSV4 VECTOR - CVSS:4.0/AV:N/AC:L/AT:N/PR:N/UI:N/VC:H/VI:H/VA:H/SC:N/SI:N/SA:N",
     "CVSSV3 BASE SCORE - 7.5",
     "CVSSV3 VECTOR - CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N",
     "CVSS BASE SCORE - 5.0",
     "CVSS VECTOR - AV:N/AC:M/Au:N/C:P/I:N/A:N"
   ]

5. Include only fields that are present in the PDF
   - If no CVSS data: cvss = []
   - If only CVSSV3: include only CVSSV3 fields
   - If multiple versions: include all that are present

📚 RULE 7 B.2 - EXTRACT REFERENCES SECTION

Search for: "Reference Information" OR "References" heading/section marker

Extraction:
  1. Find "Reference Information" section
  2. Extract ALL references:
     - CVE numbers (CVE-XXXX-XXXXX)
     - External URLs (links to advisories, patches)
     - NVD links
     - Vendor advisories
     - Other security databases
  3. Keep full URLs/CVE numbers
  4. Format as array of strings

Output:
  "references": [
    "CVE-2020-1234",
    "https://nvd.nist.gov/vuln/detail/CVE-2020-1234",
    "https://www.php.net/archive/2020.php",
    ...
  ]

❌ RULE 8 B.2 - SKIP IF:

  ❌ Heading contains "Instances (N)" - that's B.1 extraction
  ❌ No "VULNERABILITY ... PLUGIN ID" line
  ❌ NO Description/Solution/Plugin/Risk/References sections (not a base version)
  ❌ Only table of contents entry (no actual content section)

📝 RULE 9 B.2 - FIELD MAPPING FOR BASE VERSIONS

For each extracted BASE VERSION vulnerability:
  ✓ Name: "[NAME]" (exact from PDF, WITHOUT "Instances (N)")
  ✓ severity: CRITICAL | HIGH | MEDIUM | LOW | LOG
  ✓ description: [text from Description section]
  ✓ solution: [text from Solution section]
  ✓ plugin: [metadata from Plugin Details section]
  ✓ cvss: [score and details from Risk Information section]
  ✓ references: [CVE, URLs from Reference Information section]
  ✓ port: null
  ✓ protocol: null
  ✓ source: "TENABLEWAS"
  ✓ identification: [] (empty for base versions)
  ✓ http_info: [] (empty for base versions)
  ✓ detection_result, detection_method, impact, insight, product_detection_result, log_method: [] (empty for Tenable WAS)
═══════════════════════════════════════════════════════════════════════════════════
OUTPUT FORMAT
═══════════════════════════════════════════════════════════════════════════════════

🔴 CRITICAL INSTRUCTION FOR JSON OUTPUT - PAIR PATTERN MANDATORY:

OUTPUT STRUCTURE (NON-NEGOTIABLE):
  ✓ ONLY valid JSON array
  ✓ ALWAYS pairs: BASE then INSTANCES (alternating)
  ✓ EVEN number of entries (no incomplete pairs)
  ✓ Each pair: same Name (one with "Instances (N)", one without)
  ✓ Each pair: same Severity and Plugin ID

VALIDATION BEFORE OUTPUT:
  ☑ Count total entries
  ☑ If ODD number → REMOVE the incomplete last entry
  ☑ If last entry is BASE without INSTANCES → REMOVE it
  ☑ If last entry is INSTANCES without BASE before it → REMOVE it
  ☑ Only output if you have COMPLETE PAIRS

EXAMPLES OF CORRECT OUTPUT:

✓ VALID (2 pairs = 4 entries):
[
  {"Name": "PHP Unsupported Version", "description": [...], ...},
  {"Name": "PHP Unsupported Version Instances (14)", "identification": [...], ...},
  {"Name": "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities", "description": [...], ...},
  {"Name": "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities Instances (1)", "identification": [...], ...}
]

✗ INVALID (3 entries - incomplete pair - DO NOT OUTPUT):
[
  {"Name": "PHP Unsupported Version", "description": [...], ...},
  {"Name": "PHP Unsupported Version Instances (14)", "identification": [...], ...},
  {"Name": "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities", "description": [...], ...}
]
← Remove the 3rd entry (BASE without INSTANCES)

✗ INVALID (INSTANCES without BASE - DO NOT OUTPUT):
[
  {"Name": "PHP Unsupported Version Instances (14)", "identification": [...], ...}
]
← This is invalid because there's no BASE before it

✓ VALID (1 pair = 2 entries):
[
  {"Name": "PHP Unsupported Version", "description": [...], ...},
  {"Name": "PHP Unsupported Version Instances (14)", "identification": [...], ...}
]

═══════════════════════════════════════════════════════════════════════════════════
FINAL VALIDATION BEFORE OUTPUT - CRITICAL CHECKLIST
═══════════════════════════════════════════════════════════════════════════════════

Before you output JSON, you MUST validate every single pair:

STEP 1 - Count validation:
  ☐ Total entries in output = EVEN number (no ODD entries)
  ☐ Number of BASE entries = Number of INSTANCES entries
  ☐ Example: 5 BASE + 5 INSTANCES = 10 total ✓
  ☐ Example: 5 BASE + 4 INSTANCES = INVALID ✗ (Remove 1 BASE)

STEP 2 - For EACH pair in output:
  ☐ BASE Name = "X" (no "Instances (N)")
  ☐ INSTANCES Name = "X Instances (N)" (has "Instances (N)")
  ☐ BASE.Plugin_ID == INSTANCES.Plugin_ID (EXACTLY same!)
  ☐ BASE.severity == INSTANCES.severity (EXACTLY same!)
  ☐ BASE comes BEFORE INSTANCES in order

  If ANY check fails → REMOVE that pair (both BASE and INSTANCES)

STEP 3 - Verify special vulnerabilities were found:
  ☐ Searched for "Allowed HTTP Methods Instances (N)"?
  ☐ Searched for "Login Form Detected Instances (N)"?
  ☐ Searched for "Network Timeout Encountered Instances (N)"?
  ☐ Searched for "Scan Information Instances (N)"?
  ☐ Searched for "Web Application Sitemap Instances (N)"?
  ☐ Searched for "Performance Telemetry Instances (N)"?
  
  If found any, they MUST have matching BASE with LOG severity

STEP 4 - Final sanity check:
  ☐ No INSTANCES without BASE
  ☐ No BASE without INSTANCES
  ☐ All Plugin IDs match within pairs
  ☐ All Severities match within pairs
  ☐ All Names follow pattern (BASE, INSTANCES, BASE, INSTANCES...)

If ANY validation fails → Fix it before outputting JSON!

═══════════════════════════════════════════════════════════════════════════════════
EXTRACTION COUNTS EXPECTATION:
═══════════════════════════════════════════════════════════════════════════════════

CRITICAL COUNTING RULE:

Your output should have equal numbers of BASE and INSTANCES.

Count rule:
  BASE_count = number of vulnerabilities found in LOCATION 2 (base versions)
  INSTANCES_count = number of vulnerabilities found in LOCATION 1 (instances)
  TOTAL_output = BASE_count + INSTANCES_count (must be EVEN)
  
  Example:
    Found 10 INSTANCES in LOCATION 1
    Found 10 BASE in LOCATION 2
    Output: 10 BASE entries + 10 INSTANCES entries = 20 TOTAL (paired as BASE→INSTANCES)
  
  If mismatch:
    Found 10 INSTANCES in LOCATION 1
    Found only 8 BASE in LOCATION 2
    Action: Remove 2 unmatched INSTANCES from output
    Output: 8 BASE entries + 8 INSTANCES entries = 16 TOTAL

EXPECTED EXTRACTION RANGES:
  Good: 5-10 pairs (10-20 entries: 5-10 BASE + 5-10 INSTANCES)
  Better: 10-20 pairs (20-40 entries: 10-20 BASE + 10-20 INSTANCES)
  Excellent: 20+ pairs (40+ entries)

═══════════════════════════════════════════════════════════════════════════════════

Return ONLY valid JSON array with separate schemas for each source type.

OPENVAS VULNERABILITIES:
[
  {
    "Name": "<vulnerability name>",
    "description": [<array of description text or empty>],
    "detection_result": [<array or empty>],
    "detection_method": [<array or empty>],
    "product_detection_result": [<array or empty>],
    "impact": [<array or empty>],
    "solution": [<array or empty>],
    "insight": [<array or empty>],
    "log_method": [<array or empty>],
    "cvss": [<score>, none, none, none, none, none, none],
    "port": <number or null>,
    "protocol": "<tcp or udp or null>",
    "severity": "CRITICAL|HIGH|MEDIUM|LOW|LOG",
    "references": [<array or empty>],
    "plugin": null,
    "identification": [],
    "http_info": [],
    "source": "OPENVAS"
  }
]

═══════════════════════════════════════════════════════════════════════════════════
TENABLEWAS B.1 - INSTANCES VULNERABILITIES (WITH HTTP INFO):
═══════════════════════════════════════════════════════════════════════════════════

[
  {
    "Name": "XXX Instances (N)",
    "description": [],
    "detection_result": [],
    "detection_method": [],
    "product_detection_result": [],
    "impact": [],
    "solution": [],
    "insight": [],
    "log_method": [],
    "cvss": [],
    "port": null,
    "protocol": null,
    "severity": "CRITICAL|HIGH|MEDIUM|LOW|LOG",
    "references": [],
    "plugin": [],
    "identification": ["https://url1", "https://url2", ..., "https://urlN"],
    "http_info": [
      {
        "request": "GET https://url1 HTTP/1.1",
        "request_headers": ["Accept: */*", "Accept-Encoding: gzip, deflate", "User-Agent: Mozilla/5.0 ..."],
        "response_headers": ["HTTP/1.1 200 OK", "Content-Type: text/html", "Server: Apache/2.4.41"]
      },
      {
        "request": "GET https://url2 HTTP/2",
        "request_headers": [...],
        "response_headers": [...]
      }
    ],
    "source": "TENABLEWAS"
  }
]

NOTES FOR B.1 INSTANCES:
  - Name: MUST contain "Instances (N)" suffix
  - identification: array of ALL URLs from INSTANCE blocks AND Identification sections
  - http_info: array of HTTP Info entries (optional, can be empty [])
  - severity: from VULNERABILITY line (CRITICAL|HIGH|MEDIUM|LOW|LOG)
  - All other array fields: [] (empty) - EXCEPT identification and http_info
  - port and protocol: null
  - source: always "TENABLEWAS"

═══════════════════════════════════════════════════════════════════════════════════
TENABLEWAS B.2 - BASE VERSION VULNERABILITIES (WITH DESCRIPTION/SOLUTION/PLUGIN):
═══════════════════════════════════════════════════════════════════════════════════

[
  {
    "Name": "XXX",
    "description": [
      "This vulnerability occurs when...",
      "It can lead to...",
      "The affected systems..."
    ],
    "detection_result": [],
    "detection_method": [],
    "product_detection_result": [],
    "impact": [],
    "solution": [
      "Update to version X.X.X",
      "Or apply patch from vendor",
      "Verify with: curl https://..."
    ],
    "insight": [],
    "log_method": [],
    "cvss": [
      7.5,
      "CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N",
      "Base Score: 7.5 (High)",
      "Temporal Score: 7.1"
    ],
    "port": null,
    "protocol": null,
    "severity": "CRITICAL|HIGH|MEDIUM|LOW|LOG",
    "references": [
      "CVE-2020-1234",
      "https://nvd.nist.gov/vuln/detail/CVE-2020-1234",
      "https://www.example.com/advisory"
    ],
    "plugin": [
      "Plugin ID: 12345",
      "Family: Web Servers",
      "Created: 2020-01-15",
      "Updated: 2023-10-20",
      "Plugin Version: 1.2.3",
      "Check Type: Remote"
    ],
    "identification": [],
    "http_info": [],
    "source": "TENABLEWAS"
  }
]

NOTES FOR B.2 BASE VERSIONS:
  - Name: MUST NOT contain "Instances (N)" suffix - just vulnerability name
  - description: text from "Description" section of base version
  - solution: text from "Solution" section
  - plugin: metadata from "Plugin Details" section (as key: value pairs)
  - cvss: score and vector from "Risk Information" section
  - references: CVEs and URLs from "Reference Information" section
  - identification: [] (empty for base versions - no instances)
  - http_info: [] (empty for base versions - no HTTP data)
  - All OpenVAS-only fields: [] (empty) - detection_result, detection_method, product_detection_result, log_method
  - port and protocol: null
  - severity: from VULNERABILITY line
  - source: always "TENABLEWAS"

═══════════════════════════════════════════════════════════════════════════════════
MIXED OUTPUT EXAMPLE (Both B.1 and B.2 from same chunk):
═══════════════════════════════════════════════════════════════════════════════════

[
  {
    "Name": "PHP Unsupported Version Instances (14)",
    "description": [],
    "cvss": [],
    "severity": "HIGH",
    "identification": ["https://app.example.com", "https://api.example.com", ...],
    "http_info": [...14 entries from INSTANCE blocks...],
    "plugin": [],
    "solution": [],
    "source": "TENABLEWAS"
    ...other fields empty...
  },
  {
    "Name": "PHP Unsupported Version",
    "description": ["PHP has reached end-of-life...", "This version is vulnerable to..."],
    "solution": ["Update to PHP 8.1 or newer", "Contact vendor for support"],
    "plugin": ["Plugin ID: 12345", "Family: Development", ...],
    "cvss": [7.5, "CVSS:3.1/AV:N/AC:L/..."],
    "references": ["CVE-2023-0000", "https://nvd.nist.gov/..."],
    "severity": "HIGH",
    "identification": [],
    "http_info": [],
    "source": "TENABLEWAS"
    ...other fields empty...
  }
]

═══════════════════════════════════════════════════════════════════════════════════
CRITICAL EXTRACTION RULES FOR OUTPUT:
═══════════════════════════════════════════════════════════════════════════════════

1. DUAL EXTRACTION: Extract BOTH B.1 and B.2 from SAME chunks
   - If chunk has "PHP Unsupported Version Instances (14)" → create B.1 entry
   - If SAME chunk has "PHP Unsupported Version" section → create SEPARATE B.2 entry
   - Output BOTH in same JSON array

2. FIELD DIFFERENTIATION:
   - B.1 INSTANCES: identification + http_info filled, description/solution/plugin/cvss empty
   - B.2 BASE: description + solution + plugin + cvss + references filled, identification + http_info empty

3. NAME DIFFERENTIATION:
   - B.1: "XXX Instances (N)" - WITH suffix and count
   - B.2: "XXX" - WITHOUT suffix, just name

4. DO NOT SKIP:
   - Extract ALL "Instances (N)" headings for B.1
   - Extract ALL base vulnerability sections for B.2
   - If both exist in chunk, output BOTH

5. DO NOT MIX:
   - Do NOT put INSTANCE URLs in description field
   - Do NOT put description text in identification field
   - Keep B.1 and B.2 data clearly separated

