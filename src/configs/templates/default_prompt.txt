You are an information extraction model.

Your task is to extract structured vulnerability information from the TEXT REPORT provided above.

Return the output in valid JSON format using exactly these fields:

[
  {
    "Name": "<vulnerability name as found in the report>",
    "description": "<detailed full description of the vulnerability as found in the report>",
    "detection": "<detailed full detection of the vulnerability as found in the report>",
    "detection_method": "<detailed full detection method of the vulnerability as found in the report>",
    "product_detection_result": "<detailed full product detection result of the vulnerability as found in the report>",
    "impact": "<detailed full impact of the vulnerability as found in the report>",
    "insight": "<detailed full insight of the vulnerability as found in the report>",
    "cvss": "[<list of all CVSS scores found>, e.g. 'CVSSV4 BASE SCORE - number', 'CSSV4 VECTOR - string', 'CVSSv3 BASE SCORE - number', 'CVSSv3 VECTOR - string','CVSSv2 BASE SCORE - number', 'CVSS BASE SCORE - number', 'CVSS VECTOR - string', or null]",
    "severity": "<Log|Low|Medium|High|Critical>",
    "solution": "<recommended action or fix verbatim from the report>",
    "plugin_detail": "<string or null>",
    "port": "<number or null>"",
    "protocol": "<protocol|null>",
    "CWE": "[list of <CWE-ID1>, <CWE-ID2>, ... or null]",
    "WASC": "[string or null]",
    "OWASP": "[string or null]",
    "CVE": "[list of <cve-2023-1234>, <cve-2023-1235>,... or null]",
    "BID": "[string or null]",
    "source": "<OPENVAS|TENABLE_WAS>"  

  }
]

INSTRUCTIONS

INSTRUCTIONS FOR EXTRACTION

---
OPENVAS REPORTS:
1. For each "NVT" is a block of vulnerability, extract a JSON object using the fields above.
2. Use "NVT" field as "Name".
3. Use "Summary" field as "description". 
4. Port and Protocol: Extract from above the CVSS.
5. Use "Vulnerability Detection Result" as "detection".
6. Use "Vulnerability Detection Method" as "detection_method".
7. Use "Product Detection Result" as "product_detection_result".
8. Use "Impact" field as "impact".
9. Use "Solution" field as "solution".
10. Use "Vulnerability insight" field as "insight".
11. Use "CVSS" field as unique "cvss".
---
TENABLE WAS REPORTS:
1. Use "Description" field as "description".
2. Use "Name" field as "Name of vulnerabilities".
3. Example of valid Name: "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities"
4. Example of invalid Name: "Apache 2.4.x < 2.4.41 Multiple Vulnerabilities Instances", "Web Application Sitemap", "Scan Information"
5. Use "Solution" field as "solution".
6. Use "Plugin Details" field as "plugin_detail".
7. Use "Reference Information" for "CWE", "WASC", "OWASP", "CVE", "BID" fields.
8. Use "detection" field in json is "null"
9. Use "detection_method" field in json is "null"
10. Use "production_method" field in json is "null"
11. Use "impact" field in json is "null"

---
GENERAL RULES (APPLY TO BOTH):
• Output must be strictly valid JSON. No comments, markdown, or text outside the JSON array.
